import React, {Fragment} from 'react';
import {Grid} from "@mui/material";

const Profile = () => {

    return (
    <Fragment>
       <Grid></Grid>
    </Fragment>
    );
};

export default Profile;